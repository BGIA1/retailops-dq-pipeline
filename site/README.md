# RetailDQ Static Demo Site

The generated GitHub Pages demo is written to `site/generated` by:

```bash
retaildq demo --config configs/local.yaml
retaildq site-build --config configs/local.yaml
```

The checked-in `index.html` is only a placeholder. CI and the Pages workflow publish the generated synthetic demo instead.
